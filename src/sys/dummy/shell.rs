use std::{
    io,
    path::Path,
};

pub fn edit_file(_path: &Path) -> io::Result<()> {
    return Ok(());
}

pub fn open_containing_folder(_path: &Path) -> Result<(), String> {
    return Ok(());
}
